{
  "name": "paperecho",
  "scripts": {
    "check": "node -e \"const fs=require('node:fs');const path=require('node:path');const cp=require('node:child_process');function walk(dir){let out=[];for(const entry of fs.readdirSync(dir,{withFileTypes:true})){const file=path.join(dir,entry.name);if(entry.isDirectory())out=out.concat(walk(file));else if(file.endsWith('.mjs'))out.push(file);}return out;}const files=['workflow/tools','workflow/tests'].flatMap(walk);for(const file of files){const r=cp.spawnSync(process.execPath,['--check',file],{stdio:'inherit'});if(r.status)process.exit(r.status);}console.log('Checked '+files.length+' .mjs files.');\"",
    "test": "node --test workflow/tests/*.test.mjs",
    "verify": "npm run check && npm test",
    "smoke:workflow": "node workflow/tools/maintenance/smoke_check.mjs",
    "smoke:headless": "node workflow/tools/maintenance/smoke_check.mjs --mode headless",
    "smoke:desktop": "node workflow/tools/maintenance/smoke_check.mjs --mode desktop",
    "dry-run:workflow": "node workflow/tools/maintenance/workflow_dry_run.mjs",
    "dry-run:headless": "node workflow/tools/maintenance/workflow_dry_run.mjs --mode headless",
    "dry-run:desktop": "node workflow/tools/maintenance/workflow_dry_run.mjs --mode desktop"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "fast-xml-parser": "5.10.1",
    "nodemailer": "^9.0.3"
  }
}
